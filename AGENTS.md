# 项目维护入口

在分析需求、修改代码、生成安装包或发布新版本前，必须完整阅读
[`docs/product-requirements.md`](docs/product-requirements.md)，并把其中的“必须”条目作为验收条件。

- 用户明确提出的新要求优先于旧要求；实现新要求时，同步更新产品要求文档，删除或改写已经失效的旧说法。
- 不得为了初始化、升级、修复活动标记或打包而重置用户已经配置的全局元素、SQL 规则、连接配置和已保存密码。
- 发布时必须使用产品要求文档中的发布检查清单，并确认安装包文件名和内部版本号一致。
- 所有 Git commit 的提交标题和说明必须使用中文；除非用户另有明确要求，不得使用英文提交信息。
- 如果代码、README、设计规范与产品要求文档冲突，先核对当前实现和最新用户决定，再在同一次修改中消除冲突。
